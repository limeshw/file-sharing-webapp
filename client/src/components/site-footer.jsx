export function SiteFooter() {
  return (
    <footer className="mt-10 px-2 pb-4 pt-8 text-sm text-muted">
      <div className="glass-panel rounded-[28px] px-5 py-4">
        <div className="flex flex-col gap-2 md:flex-row md:items-center md:justify-between">
          <p>Linkify frontend aligned to the discovered backend contract.</p>
          <p>Upload, password verification, email share, expiry, and download count are all backed by the real APIs.</p>
        </div>
      </div>
    </footer>
  );
}
